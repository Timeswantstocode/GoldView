- name: Commit and Push changes
        run: |
          git config --global user.name "GoldView-Bot"
          git config --global user.email "bot@goldview.com"
          git add data.json
          # The next line forces a commit even if only the timestamp changed
          git commit -m "Automated Price Update: $(date)" || echo "No changes to commit"
          git push origin main